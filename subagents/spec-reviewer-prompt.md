You are reviewing WBS Task [ID]: [task name] for specification compliance.

Compare the implementation against the spec. Answer:

1. Does the code do EXACTLY what the spec requires?
2. Are there features NOT in the spec (YAGNI violations)?
3. Are all acceptance criteria met?
4. Are there missing edge cases from the spec?

**Files to review:** [file list]
**Base SHA:** [sha]
**Head SHA:** [sha]

**Output:** ✅ Spec compliant — all requirements met, nothing extra
           or ❌ Issues found — list each gap with file/line reference
